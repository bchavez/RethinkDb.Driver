ModuleName: Regression3745
TableVarNames:
- tbl
DefsAndTests:
- TestType: JavaQuery
  TestFile: regression/3745.yaml
  TestNum: 1
  Original: partial({'inserted':2})
  ExpectedType: Partial
  ExpectedJava: partial(new MapObject().with("inserted", 2))
  Obtained: tbl.insert(Arrays.asList(new MapObject().with("id", 0).with("a", 5), new MapObject().with("id", 1).with("a", 6)))
- TestType: JavaQuery
  TestFile: regression/3745.yaml
  TestNum: 2
  Original: ({'a':11})
  ExpectedType: Map
  ExpectedJava: new MapObject().with("a", 11)
  Obtained: tbl.reduce((x, y) -> r.object("a", r.add(x.bracket("a"), y.bracket("a"))))
- TestType: JavaQuery
  TestFile: regression/3745.yaml
  TestNum: 3
  Original: err('ReqlQueryLogicError','Cannot convert NUMBER to SEQUENCE')
  ExpectedType: Err
  ExpectedJava: err("ReqlQueryLogicError", "Cannot convert NUMBER to SEQUENCE")
  Obtained: tbl.reduce((x, y) -> r.expr(0).bracket(0))
